// No-op stub for Next.js "server-only"/"client-only" marker modules under Vitest.
// These markers exist to fail the build if a module is imported from the wrong
// environment; in unit tests they carry no behavior.
export {};
